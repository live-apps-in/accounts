// import { createSlice, PayloadAction } from '@reduxjs/toolkit';
// import { getThemeName, THEME } from 'src/theme';

// const initialState: THEME = getThemeName() as THEME;

// const themeSlice = createSlice({
//   name: 'theme',
//   initialState,
//   reducers: {
//     changeTheme(_state: THEME, action: PayloadAction<THEME>) {
//       return action.payload;
//     },
//   },
// });

// export const { actions: themeActions, reducer: themeReducer } = themeSlice;

export {};
